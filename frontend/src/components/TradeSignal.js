import React from 'react';

const TradeSignal = ({ signal }) => {
  return (
    <div className="TradeSignal">
      <p>{signal}</p>
    </div>
  );
};

export default TradeSignal;
import React from 'react';

const TradeSignal = ({ signal }) => {
  return (
    <div className="TradeSignal">
      <p>{signal}</p>
    </div>
  );
};

export default TradeSignal;
